このリポジトリはプロジェクト管理Webアプリケーション（仮称）のものです。
パブリックリポジトリで公開しております。

.devcontainer
このプロジェクトの開発環境を構築する際に使用するフォルダです。
devcontainer.jsonはVSCODEの拡張機能を利用し、リモート環境を作成するのに使う設定ファイルです。
Dockerfileはコンテナイメージを作成するためのファイルで、必要な機能を記述します。

projectManagementWebApp
Djangoフレームワークのフォルダです。
こちらの中で開発していきます。

db.splite3
db.sqlite3 は、Djangoプロジェクトで使用されるデフォルトのデータベースファイルです。
SQLiteという軽量なデータベースエンジンが組み込まれており、データベースのテーブルやレコードなどのデータが格納されます。
Djangoでは、データベースモデルを定義してデータベースとやり取りするために使用されます。
開発中のプロジェクトでは、デフォルトの設定で db.sqlite3 ファイルが作成され、データベースの構造や
実際のデータがこのファイルに保存されます。

manage.py
manage.py は、Djangoプロジェクトの管理スクリプトです。
Djangoのコマンドラインツールとして機能し、さまざまな開発タスクを実行するために使用されます。
プロジェクトのディレクトリ内にあり、Djangoのコマンド（例えば、データベースのマイグレーション、
サーバーの起動、テストの実行など）を実行する際に python manage.py の後にコマンドを追加して使用します。